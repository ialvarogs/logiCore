// prisma.ts
import { PrismaClient } from "@prisma/client"; // Importamos la clase PrismaClient de Prisma.

const prisma = new PrismaClient(); // Creamos una instancia del cliente de Prisma.

export default prisma; // Exportamos la instancia de Prisma para usarla en otras partes del proyecto.
